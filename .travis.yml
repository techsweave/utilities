language: node_js

node_js:
  - 14

install:
  - npm install -g typescript
  - npm install

script:
  - npm run build

deploy:
  provider: npm
  email: techsweave@gmail.com
  api_key: $NPM_TOKEN
  on:
    branch: main

branches:
  only:
    - main